#include <stdio.h>

// 변수 이름은 63자로 제한
int main() {
  // 정수 타입  
  int x = 10, y = 20, z;
  int age, youngAge, young_age;

  int abc, Abc, ABC;

  short int age1;
  short     age2;

  long long age100;

  // 실수 타입
  float pi = 3.14;
  double pi_precision = 3.1415926535897932;

  // 양수
  unsigned int u_i = 123;

  // 가장 왼쪽의 비트 MSB
  // 가장 오른쪽의 비트 LSB

  // sizeof 크기를 알려준다.

  // typedef 기존 타입을 새 이름으로 정의

  typedef unsigned short int WORD;
  WORD wAge;

  return 0;  


}